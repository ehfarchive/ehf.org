import { primaryNavigation, type SiteLink, type SiteNavigationItem } from '../data/site';

export type NavigationChild = SiteLink;
export type NavigationItem = SiteNavigationItem;
export const navigationItems = primaryNavigation;
